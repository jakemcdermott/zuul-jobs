- pragma:
    implied-branch-matchers: True
    implied-branches:
      - master
      - devel

- project-template:
    name: python
    check:
      jobs:
        - tox:
            nodeset: static-node
            vars:
              tox_envlist: ALL
    gate:
      jobs:
        - tox:
            nodeset: static-node
            vars:
              tox_envlist: ALL

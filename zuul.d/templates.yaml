- pragma:
    implied-branch-matchers: True
    implied-branches:
      - master
      - devel

- project-template:
    name: python
    check:
      jobs:
        - tox:
            nodeset: static-node
            vars:
              tox_envlist: ALL

- project-template:
    name: python-awx
    check:
      jobs:
        - tox
    gate:
      jobs:
        - tox

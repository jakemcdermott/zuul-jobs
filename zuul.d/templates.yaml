- pragma:
    implied-branch-matchers: True
    implied-branches:
      - master
      - devel

- project-template:
    name: python
    check:
      jobs:
        - tox:
            vars:
              tox_envlist: ALL
    gate:
      jobs:
        - tox:
            vars:
              tox_envlist: ALL

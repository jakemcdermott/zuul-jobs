- job:
    name: flake8
    description: |
      Invoke and run flake8
    run: playbooks/flake8.yml

- job:
    name: pipenv-tox
    description: |
      Run Tox through pipenv
    run: playbooks/tox-pipenv.yml
